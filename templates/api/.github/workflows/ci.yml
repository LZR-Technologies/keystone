name: CI
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  ci:
    uses: ./.github/workflows/_ci-reusable.yml
    with:
      node-version: '20'
      package-manager: 'npm'
